
* updatedDir, updatedSp

* what happen call intersect when each is same

* same arg
dir same dim2
dir same Dir(dim2)

* NonZeroPoint
Dir(what dir), Range(?)

* intersect
rect|rect, circle|rect

* contain Figure
Figure intersect Point = Figure contain pt
